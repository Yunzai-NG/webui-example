# 更新日志

格式参照 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## 0.2.0 — 2026-08-30

### 新增

- **日历组件**：周数与周几、大日号、公历与年内天序、农历月日、干支三柱、6×7 月历网格、年月周
  三条剩余进度、距离春节。农历由 node 侧的 `lunar-typescript` 算，依赖装不上时只有那一条路由
  失败，组件退回只显示公历。
- **本包自带 `style.css`**（`webuiPanel.style`）：关键帧、月历网格与伪元素写不进行内样式。

### 变更

- **一言改为引言排版**：大引号、居中斜体的句子、出处行，左下角两枚图标按钮（换一句 / 复制）。
  复制在 `http://` 下走 `execCommand` 退路 —— 非安全上下文里没有 `navigator.clipboard`，直接
  调用是静默失败。
- **时钟收窄为纯报时**，日期类事实全归日历。每秒重画的与一天变一次的不该合成一张卡片。
- **配置从六项收敛成一项**（一言接口）：其余五项要么已由组件自行决定，要么随时钟的收窄失去了
  对应物。
- 一言的取数放宽字段识别（`hitokoto` / `text` / `content` 任一皆可，出处同理），且上游失败改为
  返回 `{ error }` 而非 500 —— 500 的含义是服务端有 bug，第三方接口挂掉不是。
- README：面板商店此前把「要不要装依赖」做成确认框里的一问，现在缺省就装，故「会问你要不要
  一并装上」不再成立。

### 修复

- `clock.js` 用到的 `.clock-sec` 在面板样式表里并不存在，那一行一直是无样式的纯文本。本包自带的
  `style.css` 补上了它。

## 0.1.0 — 2026-08-29

面板插件示例包：三枚组件、一个页签、一份 node 侧入口、六项配置。挑这几样是因为它们各示范一件
写面板插件时会遇到的事。

无构建步骤，装完刷新页面即生效；带 node 侧那一半需重载一次 webui。
